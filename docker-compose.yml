services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: mdgraph
      POSTGRES_PASSWORD: mdgraph
      POSTGRES_DB: mdgraph
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
